name: "NetDuid CodeQL Configuration"

query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
        - note

paths-ignore:
  - src/NetDuid.Tests
  - src/NetDuid.Benchmarks
